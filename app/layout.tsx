import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "ESENet Job Fair 2024 - Connecting Talent with Opportunity",
  description: "Rejoignez-nous à l'ESENet Job Fair 2024 pour des opportunités de réseautage, des présentations d'entreprises et des panels de conférenciers. Connectez-vous avec des leaders de l'industrie et trouvez votre prochaine opportunité de carrière.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
